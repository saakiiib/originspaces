---
paths:
  - 'resources/views/**/*.blade.php'
---

# Views

## Use x-icon component for icons
Render icons with the <x-icon name="phone" class="..." /> component (server-side SVG from icons.js), never raw <i data-lucide>. The only exception is names only known at JS runtime.
